{
  "name": "projectName",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "npm-run-all -s clean parcel:dev",
    "build": "npm-run-all -s clean parcel:build",
    "parcel:dev": "npx parcel src/*.html --open",
    "parcel:build": "NODE_ENV=production parcel build ./src/*.html",
    "clean": "rm -rf dist .cache"
  },
  "author": "dazza",
  "dependencies": {
    "autoprefixer": "^9.8.5",
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.12.4",
    "posthtml-expressions": "^1.4.5",
    "posthtml-include": "^1.4.3",
    "serve": "^11.3.2",
    "tailwindcss": "^1.4.6"
  }
}

